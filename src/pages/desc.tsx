import React from "react";
import { myComponent } from './desc.module.css';

export default () =>{
    return(
        <div className={myComponent}>
            How with component styling
        </div>
    );
}